If you update your flake file:
```
nix flake update
```

Update the configuratin:
```
sudo nixos-rebuild switch --flake .
```

Updating home.nix:
```
nix run home-manager/master -- switch -b backup  --flake .
```
